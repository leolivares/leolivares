<%= form_with(model: subscription, local: true) do |form| %>
  <% if subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
      <% subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :follower_id %>
    <%= form.number_field :follower_id, id: :subscription_follower_id %>
  </div>

  <div class="field">
    <%= form.label :followed_id %>
    <%= form.number_field :followed_id, id: :subscription_followed_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
